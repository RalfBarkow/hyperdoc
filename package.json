{
  "name": "hyperdoc",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test:playwright": "playwright test -c tests/playwright/playwright.config.js",
    "test:playwright:nor": "playwright test -c tests/playwright/playwright.config.js tests/playwright/nor-matcher-demo.spec.js --project=chromium"
  },
  "devDependencies": {
    "@playwright/test": "1.50.1"
  }
}
